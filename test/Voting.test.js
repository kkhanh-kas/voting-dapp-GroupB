/**
 * Chứa 6 test cases tự động như sau: 
 * Contract khởi tạo đúng số ứng viên
 * Thông tin ứng viên ban đầu đúng (voteCount = 0)
 * Vote thành công → voteCount tăng lên 1
 * Từ chối vote cho ứng viên id không tồn tại
 * Từ chối vote 2 lần từ cùng địa chỉ
 * Event votedEvent được phát đúng
 */